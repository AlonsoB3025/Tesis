# Este archivo define todas las dependencias necesarias para reproducir el 
# pipeline de tu tesis. Está dividido en secciones lógicas, con comentarios para 
# facilitar su comprensión y mantenimiento.


# Core scientific stack
numpy>=1.26              # Operaciones numéricas y base de cálculo matricial
pandas>=2.2              # Manipulación de datos tabulares (DataFrames)
scikit-learn>=1.4        # Modelos ML supervisados/no supervisados, métricas, pipelines
scipy>=1.13              # Funciones estadísticas, álgebra lineal y optimización
imbalanced-learn>=0.12   # Técnicas para tratar desbalance de clases (SMOTE, undersampling)


# Data I/O & Serialization
pyarrow>=14.0            # Lectura/escritura eficiente (Parquet, Feather)
python-dotenv>=1.0.0     # Manejo de variables de entorno (.env) para credenciales y rutas
joblib>=1.3              # Serialización de modelos y transformadores


# Visualization & Reporting
matplotlib>=3.8          # Visualizaciones: curvas ROC, matrices de confusión, histogramas
seaborn>=0.13            # Visualización estadística avanzada (EDA)
shap>=0.44               # Explicabilidad de modelos ML (SHAP values)


# Notebooks & Execution
jupyter>=1.0             # Entorno interactivo de notebooks
ipykernel>=6.29          # Kernel de notebooks para ejecución en entornos virtuales
papermill>=2.5           # Ejecución programática de notebooks con parámetros


# Logging & utilities
rich>=13.0               # Logging enriquecido, con formato legible en consola/archivo
